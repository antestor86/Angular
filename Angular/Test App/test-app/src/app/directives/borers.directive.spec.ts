import { BorersDirective } from './borers.directive';

describe('BorersDirective', () => {
  it('should create an instance', () => {
    const directive = new BorersDirective();
    expect(directive).toBeTruthy();
  });
});
