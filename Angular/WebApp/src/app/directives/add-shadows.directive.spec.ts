import { AddShadowsDirective } from './add-shadows.directive';

describe('AddShadowsDirective', () => {
  it('should create an instance', () => {
    const directive = new AddShadowsDirective();
    expect(directive).toBeTruthy();
  });
});
