import { ActivebuttonDirective } from './activebutton.directive';

describe('ActivebuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new ActivebuttonDirective();
    expect(directive).toBeTruthy();
  });
});
